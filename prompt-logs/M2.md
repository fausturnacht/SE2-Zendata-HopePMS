# M2 - Frontend Developer (UI/UX) Prompt Log – Nerecina, John Lian R.

**Project:** Hope Inc. Product Management System (HopePMS)  
**Branch Strategy:** main, dev, feature/*, fix/*  
**Role:** Frontend Developer (M2)

---

**Entry 1**  
**Date:** March 30, 2026  
**Task:** Sprint 1 - Overall Frontend Plan & User Stories  
**Prompt given to AI:** "My role here is Frontend designer make me an outline for me to become a successful front end designer of our team... Include also all the moves and things that I need to do with our github repository... In creating my wireframes I want to use stitch..."

**What the AI produced:**  
Complete 6-week detailed outline for M2 role including Git workflow, PR titles, commit conventions, and Stitch prompts for all major pages.

**What I changed / improved:**  
Adapted the plan to use Google Authentication only (no email/password registration).  

**What I learned / decided:**  
Will follow the structured PR approach (4 PRs in Sprint 1) and use Stitch AI for all wireframes.

---

**Entry 2**  
**Date:** April 04, 2026  
**Task:** feat/ui-login-page — Login page with Google Authentication only (PR-01)  
**Prompt given to AI:** Multiple prompts covering implementation steps, Tailwind styling to match Stitch, handling "vite not found" error, Codespaces 5173 port 404 issue, and final code for Google-only LoginPage.

**What the AI produced:**  
Full `LoginPage.jsx` code (Google-only), detailed setup instructions for Vite/Codespaces, supabaseClient.js template, and comprehensive troubleshooting for npm/git/Codespaces issues.

**What I changed / improved:**  
Applied the Stitch-generated design directly. Removed all email/password fields. Uploaded wireframes to `wireframes/` folder.

**What I learned / decided:**  
Google login is now the only authentication method. Successfully resolved common Codespaces + Vite setup problems.

---

**Entry 3**  
**Date:** April 10, 2026  
**Task:** Pull Request Creation (PR-01)  
**Prompt given to AI:** "Im done with this: For PR-01 (Login Page – Google only): make me a pr title and description."

**What the AI produced:**  
Professional PR Title and Description template (including mention of Stitch code and wireframes folder).

**What I changed / improved:**  
Updated description to reflect that Stitch code was applied to duplicated dev branch and wireframes were uploaded.

**What I learned / decided:**  
Clear PR descriptions with screenshots and references help M1 and M4 review faster.

---

**Entry 4**  
**Date:** April 10, 2026  
**Task:** Sprint 1 User Stories Update  
**Prompt given to AI:** "Edit the sprint 1 revise the pr1 and pr2 because we are going to use google authentication as login only..."

**What the AI produced:**  
Revised user stories for Sprint 1 with updated PR titles and new Stitch prompts for Google-only login.

**What I changed / improved:**  
Fully adapted Sprint 1 to Google Authentication only while keeping 4 PRs.

**What I learned / decided:**  
Flexibility in authentication method is acceptable as long as sprint deliverables (4 PRs) are still met.

---

**Entry 6**
**Date:** April 18, 2026
**Task:** `feat/ui-product-list` — ProductListPage with stamp gating and status filter

**Prompt given to AI:**
> I have attached a Stitch-generated HTML wireframe file for the ProductListPage.
>
> Create `src/pages/ProductListPage.tsx` as a React + Tailwind CSS component that replicates the Stitch design. Include: page title "Products" + "Add Product" button (hidden if user lacks ADD_PRODUCT right via `useRights()`), full-width search bar, data table with columns: Product Code, Description, Unit, Current Price, Stamp (hidden if user lacks STAMP right), Actions (Edit + Trash icon Delete button per row). Edit and Delete buttons hidden/disabled based on `useRights()` checks. Pagination controls at the bottom. Sidebar "Deleted Items" link visible only for ADMIN and SUPERADMIN roles. Responsive: full table on desktop, horizontal scroll on mobile. Wire table data to the existing `/api/products` endpoint. Use existing project conventions: TypeScript, Tailwind CSS, Manrope/Inter fonts, existing sidebar and layout wrapper from Sprint 1. After applying the design, delete the attached HTML wireframe file from the repository.

**What the AI produced:**
`ProductListPage.tsx` was successfully generated with the correct table structure, rights-gated columns and buttons, search bar, pagination, and responsive layout. The Stitch wireframe design was faithfully applied and the HTML file was deleted after implementation.

**What I changed / improved:**
Verified that `useRights()` correctly hid the Stamp column and Add/Edit/Delete buttons for standard users. Confirmed the "Deleted Items" sidebar link only appeared for ADMIN and SUPERADMIN roles.

**What I learned / decided:**
The rights matrix integration works as expected at the UI level. This page serves as the foundation for all Sprint 2 product CRUD modals. M5 was flagged to confirm all 18 rights tests on this page before the Sprint 2 gate.

---

**Entry 7**
**Date:** April 26, 2026
**Task:** `feat/ui-product-crud` — AddProductModal, EditProductModal, SoftDeleteConfirmDialog

**Prompt given to AI:**
> I have attached three separate Stitch-generated HTML wireframe files for the three product CRUD modals.
>
> 1. **AddProductModal** — Create `src/components/products/AddProductModal.tsx`. Modal overlay with dark backdrop, title "Add Product", fields: Product Code (max 6 chars, live character counter), Description (max 30 chars, live character counter), Unit dropdown (pc, ea, mtr, pkg, ltr). Field-level validation with red border + error message. Footer: Cancel + Save button. POST to `/api/products`. Loading state on Save during request.
>
> 2. **EditProductModal** — Create `src/components/products/EditProductModal.tsx`. Same layout as Add but accepts a `product` prop to pre-fill all fields. Title: "Edit Product". Product Code read-only. PATCH to `/api/products/:id`. Loading state on Save Changes.
>
> 3. **SoftDeleteConfirmDialog** — Create `src/components/products/SoftDeleteConfirmDialog.tsx`. Small modal, warning icon, title "Delete Product?", body references `prodCode` prop. Cancel (outlined) + Confirm Delete (solid red) buttons. PATCH to `/api/products/:id/soft-delete`. Do NOT use hard DELETE. Loading state on Confirm Delete.
>
> Integrate all three into `ProductListPage.tsx` triggered by their respective action buttons. After applying the designs, delete all three HTML wireframe files.

**What the AI produced:**
All three modal components were generated correctly with proper validation, character counters, loading states, and correct API methods. SoftDelete correctly used PATCH instead of hard DELETE. All components were integrated into ProductListPage and the wireframe files were deleted.

**What I changed / improved:**
Double-checked that the soft delete endpoint was PATCH and not DELETE to preserve data integrity for the recovery flow in PR04. Confirmed Cancel buttons did not trigger any API calls.

**What I learned / decided:**
Separating the three modals into individual component files keeps the codebase clean and makes each one independently testable. Character limits on Product Code and Description enforce the constraints required by the Product API.

---

**Entry 8**
**Date:** April 27, 2026
**Task:** `feat/ui-price-history` — PriceHistoryPanel + AddPriceEntryForm

**Prompt given to AI:**
> I have attached a Stitch-generated HTML wireframe file for the PriceHistoryPanel.
>
> Create `src/components/products/PriceHistoryPanel.tsx` as a React + Tailwind CSS accordion component. Collapsed state: chevron/expand arrow on the product row. Expanded state: "Price History" table with columns Effective Date and Unit Price, fetched from `/api/price-history?productId=:id` on expand. Each product row manages its own independent open/close state. Below the history table: inline "Add New Price" form with Date Picker, Price input, and right-aligned Save button. POST to `/api/price-history` with `productId`, `effDate`, `unitPrice`. Loading and error states for both fetch and save. Spinner on Save while POST is in flight. Table refreshes on success. Integrate the accordion into `ProductListPage.tsx`. After applying the design, delete the HTML wireframe file.

**What the AI produced:**
`PriceHistoryPanel.tsx` was generated with correct accordion behavior, independent open/close state per row, lazy fetching on expand, and a working inline Add New Price form. Loading and error states were applied to both the fetch and the POST.

**What I changed / improved:**
Confirmed that each row's accordion toggled independently without affecting other rows. Verified the table refreshed automatically after a new price entry was saved without requiring a full page reload.

**What I learned / decided:**
Fetching price history only on expand keeps the initial ProductListPage load fast — important as the product list grows. The inline form avoids a separate page navigation, keeping the workflow efficient for users logging frequent price changes.

---

**Entry 9**
**Date:** April 27, 2026
**Task:** `feat/ui-deleted-items` — DeletedItemsPage with Recover button

**Prompt given to AI:**
> I have attached a Stitch-generated HTML wireframe file for the DeletedItemsPage.
>
> Create `src/pages/DeletedItemsPage.tsx`. Page header: "Archived Products" with subtitle "(ADMIN / SUPERADMIN only)". Gate the entire page via `useRights()` — redirect to `/products` if user does not have ADMIN or SUPERADMIN role. Data table: Product Code, Description, Stamp, Recover button (green outlined, per row). No Add or Edit buttons anywhere on this page. Recover: PATCH to `/api/products/:id/recover`, then refresh the table. Sidebar "Deleted Items" link highlighted/active on this page. Responsive: horizontal scroll on mobile. Register as a protected route in `App.tsx`. Use existing layout wrapper. After applying the design, delete the HTML wireframe file.

**What the AI produced:**
`DeletedItemsPage.tsx` was generated with the correct table, rights gate, Recover button wired to the correct PATCH endpoint, active sidebar link, and mobile responsive layout. The page was registered as a protected route in `App.tsx`.

**What I changed / improved:**
None.

**What I learned / decided:**
This page closes the soft-delete loop started in PR02 — deleted products are never permanently lost and can always be recovered by an authorized admin. Confirmed with the team that M5 sign-off on all 18 rights tests is required before the Sprint 2 gate is considered passed.

---

**Entry 10**
**Date:** May 1, 2026
**Task:** `feat/ui-reports` — ReportsPage with REP-001, REP-002 subtabs and PDF export

**Prompt given to AI:**
> I have attached Stitch-generated HTML wireframe files for the Reports page.
>
> Create `src/pages/reports/ReportsPage.tsx` as a tabbed page with two subtabs: REP-001 and REP-002. Fetch data from the existing `reports.ts` API already merged into dev.
>
> **REP-001 — Full Product Listing:** table identical to ProductListPage but with NO Actions column. Columns: Product Code, Description, Unit, Current Price (live from `/api/products`), Stamp (conditionally via `useRights()`). Search/filter, sortable columns, pagination. "Export CSV" button — client-side CSV of current filtered/sorted data. "Export PDF" button — structured PDF via jsPDF + jsPDF-AutoTable: Header (HOPE PMS logo, report title, generated date, page number), Content (product table), Footer (system label, "Confidential", page X of Y).
>
> **REP-002 — Top Selling Products:** horizontal bar chart via Recharts `BarChart` (fallback to Tailwind CSS bars). Each bar labeled with Product Code and total sold value. Summary table: Rank, Product Code, Description, Total Sold. Toggle between chart and table-only view.
>
> Gate both tabs via `useRights()` — REP_001 and REP_002 rights. Register as protected route in `App.tsx`. After applying, delete all HTML wireframe files.

**What the AI produced:**
`ReportsPage.tsx` was generated with working tab switching, REP-001 table with CSV and PDF export, REP-002 bar chart with Recharts and a toggle to table view, and correct per-tab rights gating. PDF output included the correct header, content, and footer structure per the REP-003 requirement.

**What I changed / improved:**
Verified the PDF export produced a properly structured document with logo, title, date, and page numbers in the header and "Confidential" label in the footer. Confirmed users with only one report right could still access their permitted tab.

**What I learned / decided:**
Combining REP-001 and REP-002 into a single tabbed page keeps the sidebar clean and groups all reporting functionality in one place. The REP-003 PDF export requirement was satisfied as a feature of REP-001 rather than a separate page, which aligns with what the dev specified.

---

**Entry 11**
**Date:** May 1, 2026
**Task:** `feat/ui-admin-users` — UserManagementPage with pre-authorization and SUPERADMIN row protection

**Prompt given to AI:**
> I have attached a Stitch-generated HTML wireframe file for the UserManagementPage.
>
> Create `src/pages/admin/UserManagementPage.tsx` with two sections wired to the existing `users.ts` manage-users API already merged into dev.
>
> **Section 1 — Pending Authorization:** fetch from `/api/users/pending`. Table: User ID, Username, Email, Requested Role, Date Requested, Actions. Approve (green, PATCH `/api/users/:id/approve`) + Reject (red, PATCH `/api/users/:id/reject`). Loading states on both buttons. Empty state: "No pending authorization requests."
>
> **Section 2 — Active Users:** fetch from `/api/users`. Table: User ID, Username, User Type, Status, Actions. Activate (PATCH `/api/users/:id/activate`) + Deactivate (PATCH `/api/users/:id/deactivate`). SUPERADMIN rows: both buttons permanently disabled (grayed out, `cursor-not-allowed`) with tooltip "SUPERADMIN accounts cannot be modified" — enforced regardless of current user role.
>
> Gate entire page via `useRights()` with ADM_USER right. Sidebar "Admin" link visible and active only for ADM_USER right holders. Register as protected route in `App.tsx`. After applying, delete the HTML wireframe file.

**What the AI produced:**
`UserManagementPage.tsx` was generated with both sections correctly separated, pre-authorization flow wired to the existing API, SUPERADMIN row protection enforced in the UI, and ADM_USER rights gate active. The page was registered as a protected route.

**What I changed / improved:**
Verified that the SUPERADMIN row protection was enforced at the UI level regardless of the current user's own role — even a logged-in SUPERADMIN cannot modify another SUPERADMIN account through this interface. Confirmed the pre-authorization flow correctly moved approved users into the Active Users section without a full page reload.

**What I learned / decided:**
The pre-authorization requirement is critical for R&A compliance — no user should be able to access the system without an explicit admin approval step. The dev confirmed this was already handled in `users.ts` so no new endpoints were needed, only the UI wiring.

---

**Entry 12**
**Date:** May 1, 2026
**Task:** `fix/ui-polish` — Loading states, empty states, error handling and responsive polish

**Prompt given to AI:**
> I have attached a Stitch-generated HTML wireframe file for loading, empty, and error states.
>
> Extract two reusable components: `src/components/shared/SkeletonTable.tsx` (animate-pulse gray placeholder rows, accepts `columns` prop) and `src/components/shared/EmptyState.tsx` (centered SVG illustration + message, accepts `message`, `ctaLabel`, `onCtaClick` props).
>
> Apply across ALL pages — ProductListPage, DeletedItemsPage, ReportsPage, UserManagementPage:
> - **Loading:** `SkeletonTable` while any API call is in flight
> - **Empty states** with contextual messages: ProductListPage ("No products found. Add your first product to get started." + CTA), DeletedItemsPage ("No archived products."), ReportsPage REP-001 ("No products available for this report."), REP-002 ("No sales data available."), UserManagementPage pending ("No pending authorization requests."), active ("No active users found.")
> - **Error state:** inline red banner + Retry button on all pages
>
> Mobile fixes at 375px and 768px: sidebar collapses to icon-only rail, all tables scroll horizontally, modals/dialogs/accordion verified usable. No duplicated markup — all pages must import `SkeletonTable` and `EmptyState`. After applying, delete the HTML wireframe file.

**What the AI produced:**
`SkeletonTable.tsx` and `EmptyState.tsx` were extracted as reusable shared components and applied consistently across all pages. All loading, empty, and error states were implemented with the correct contextual messages per page. Mobile responsiveness issues were resolved at both 375px and 768px breakpoints, and the sidebar correctly collapsed to an icon-only rail on mobile.

**What I changed / improved:**
Audited the codebase to confirm no inline duplicates of skeleton or empty state markup existed. Verified the Retry button successfully re-triggered the correct API call on each page.

**What I learned / decided:**
Extracting `SkeletonTable` and `EmptyState` as shared components early pays off — any future pages added to the project can reuse them immediately with no extra work. This is the final PR of Sprint 3 and the app is now production-ready for the demo across all device widths.
